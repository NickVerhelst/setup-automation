# Check if there are any duplicate packages
# Report on duplicates in shell
$fileLocation = "Location of file to check"
$fileName = "name"
$outputLocation = "./logs/$fileName"

foreach($line in Get-Content $fileToCheck) {
    Write-Output
}

